{% for server in groups['minio'] %}
[{{ hostvars[server]["inventory_hostname_short"] }}]
type = s3
env_auth = false
access_key_id = {{ minio_root_user }}
secret_access_key = {{ minio_root_password }}
endpoint = http://{{ hostvars[server]["ansible_host"]}}:9000

{% endfor -%}